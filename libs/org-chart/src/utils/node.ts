import { OrgChartNode } from '../models/node';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const addNode = (node: Partial<OrgChartNode>, parentNodeId: string, nodes: Record<string, OrgChartNode>) => {
  // const parentNode = nodes[parentNodeId];
  // if (!parentNode) {
  //   return;
  // }

  // const newChildrenContainerSpace = getChildrenContainerWidth(parentNode) + getNodeWidth(node) + CHILDREN_GAP;

  // const isLeftConflictSize = parentNode.ocEdgeRightNodeId
  // if () {

  // }
}
